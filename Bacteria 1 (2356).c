#include <stdio.h>

int main(void){

    char  strD[101], strS[101];

    strstr(strD, strS);

    while(scanf("%s", strD) != EOF){
        
        scanf("%s", &strS);

        strstr != NULL ? printf("Resistente\n") : printf("Nao resistente\n");
    
    }

    return 0;
}